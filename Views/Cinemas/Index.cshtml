@*Define the model/s that you're working with*@
@model IEnumerable<Cinema>

@*Define the ViewData, Which should display the data*@
@{
    ViewData["Title"] = "Cinema";
}

@*Actual code that would be shown*@
<div class="row">
    <div class="col-md-10 offset-md-2">
        <table class="table">
            <thead>
                <tr>
                    <th class="align-center">@Html.DisplayNameFor(model => model.CinemaImagePath)</th>
                    <th>@Html.DisplayNameFor(model => model.CinemaName)</th>
                    <th>@Html.DisplayNameFor(model => model.CinemaDescription)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="align-self-center">
                            <img src="@item.CinemaImagePath" alt="@item.CinemaName" style="max-width: 150px;" />
                        </td>
                        <td class="align-middle">
                            <h4 class="text-dark">@item.CinemaName</h4>
                            @Html.DisplayFor(ModelItem => item.CinemaName)
                        </td>
                        <td class="align-middle">
                            <h4 class="text-dark">@item.CinemaDescription</h4>
                            @Html.DisplayFor(ModelItem => item.CinemaDescription)
                        </td>
                        <td class="align-middle">
                            <a class="btn btn-outline-primary"><i class="bi bi-pencil-square"></i> Edit </a> ||
                            <a class="btn btn-outline-success"><i class="bi bi-eye"></i> View </a> ||
                            <a class="btn btn-outline-danger"><i class="bi bi-trash"></i> Delete </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>